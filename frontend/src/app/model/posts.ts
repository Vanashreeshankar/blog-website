export class Post {
    _id!: string;
    title!: string;
    description!: string;
    auther!: string;
    date?: Date;

    //constructor(public title:string, public description:string, public auther:string) {}
}
